export const environment = {
    production: true,
    enableDebug: false,
    url: 'https://github.com/jldblog/lfpe',
    name: require('../../package.json').name,
    title: require('../../package.json').title,
    title1: require('../../package.json').title1,
    title2: require('../../package.json').title2,
    version: require('../../package.json').version,
};
